# النسخ الاحتياطي والاسترجاع — SuperMarket Cloud

نظام متعدد المستأجرين: فقدان قاعدة البيانات = فقدان بيانات كل العملاء. **النسخ الاحتياطي إلزامي قبل النشر.**

---

## ⚠️ أنت حالياً على خطة Neon المجانية (بلا PITR)
الخطة المجانية **لا تتيح Point-in-Time Recovery**. هذا يعني أن **نسخ `pg_dump` المنطقية أدناه هي وسيلة الاسترجاع الوحيدة لديك** — تعامل معها كأنها حبل النجاة. لا تؤجّر النظام قبل أن تعمل النسخ يومياً + نسخة خارجية.

> ملاحظة الخطة المجانية: «Scale to zero بعد 5 دقائق» — القاعدة تنام عند الخمول. السكربت يوقظها تلقائياً ويعيد المحاولة مرة واحدة عند الصحوة.

---

## 1. النسخ المنطقية (الأساسية الآن)

### التشغيل اليدوي
```bash
npm run backup:db        # → backups/db-<timestamp>.dump (مضغوط)
```
- يتطلب **PostgreSQL client tools** مثبّتة. السكربت **يكتشف `pg_dump` تلقائياً** في `C:\Program Files\PostgreSQL\*\bin` ولو لم يكن على PATH. (لمسار مخصّص: ضع `PG_BIN_DIR` في `.env`.)
- يستخدم تلقائياً الاتصال **المباشر** بـ Neon (يزيل `-pooler` و`pgbouncer`) المطلوب لـ pg_dump.
- مجلد `backups/` متجاهَل في git.

### الضبط بمتغيّرات بيئة (بلا تعديل كود)
أضِف في `.env` عند الحاجة:
```
BACKUP_RETENTION_DAYS=14
BACKUP_UPLOAD_CMD=rclone copy "{file}" gdrive:supermarket-backups
```
- `BACKUP_RETENTION_DAYS`: كم يوماً تحتفظ بالنسخ (افتراضي 14).
- `BACKUP_UPLOAD_CMD`: أمر يُنفَّذ بعد كل نسخة لرفعها خارجياً (`{file}` = مسار الملف).

### 🔴 نسخة خارجية (مهم على الخطة المجانية)
النسخ تُحفظ على قرص الجهاز. لو تعطّل القرص، تضيع النسخ مع القاعدة. اختر أحد الحلّين:
- **الأسهل**: اجعل مجلد `backups/` داخل مجلد مزامنة سحابي (OneDrive / Google Drive).
- **الأمتن**: اضبط `BACKUP_UPLOAD_CMD` لرفعها إلى تخزين سحابي (rclone / aws s3 cp …).

## 2. الجدولة اليومية (Windows Task Scheduler)
Create Basic Task → Daily → Start a program:
- Program: `node`
- Arguments: `scripts/backup-db.js`
- Start in: مجلد المشروع
> Linux/خادم: cron → `0 2 * * * cd /app && node scripts/backup-db.js`

## 3. الاسترجاع
```bash
npm run restore:db backups/db-<timestamp>.dump
```
⚠️ **مدمّر** (`--clean`). اختبره على فرع Neon تجريبي أولاً، لا على الإنتاج.

## 4. اختبار الاسترجاع (إلزامي شهرياً)
نسخة لم تُختبر = لا نسخة:
1. أنشئ فرع Neon تجريبي (المجاني يتيح حتى 10 فروع).
2. استرجع آخر نسخة إليه.
3. تأكد أن الجداول والبيانات سليمة.

## 5. النسخ المحلي على أجهزة الكاشير (SQLite)
مبني ويعمل تلقائياً: الـ main process في Electron يأخذ نسخة JSON كل 6 ساعات + عند الإغلاق، تُحفظ في `userData/backups/`. لا حاجة لإعداد. (فعّل BitLocker أيضاً — انظر `DEPLOYMENT.md`.)

---

## 6. 🚀 مسار الترقية → PITR (عند تطوير خطتك)
PITR **يُفعَّل من لوحة Neon، لا من الكود** — لذا التكامل فوري بلا أي تغيير برمجي:

1. رقّ خطة Neon (Launch/Scale).
2. Neon Console → مشروعك → **Settings → Restore / History retention** → فعّل الاحتفاظ (7 أيام Launch، حتى 30 يوماً Scale).
3. صار لديك الآن استرجاع لأي لحظة (شبكة أمان فورية).
4. **لا تُلغِ النسخ المنطقية** — أبقِها كطبقة ثانية، ويمكنك فقط **تقليل تكرارها** (مثلاً أسبوعياً) عبر تعديل جدولة Task Scheduler — **بلا أي تغيير في الكود**.

> الكود مهيّأ مسبقاً: نظام النسخ مستقل تماماً عن الخطة، والضبط كله عبر متغيّرات بيئة + الجدولة. ترقيتك لا تتطلب أي تعديل برمجي.
