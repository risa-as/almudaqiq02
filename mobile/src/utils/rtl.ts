/**
 * ثوابت ترتيب الواجهة العربية.
 *
 * التطبيق يعمل بتخطيط **LTR ثابت** (انظر app/_layout.tsx). سبب ذلك أن فرض RTL عبر
 * `I18nManager.forceRTL(true)` يرفع علم `isRTL` في جافاسكربت فورًا بينما يبقى رسم
 * Yoga أفقيًا LTR حتى إعادة تشغيل التطبيق — فيكذب العلم على الأنماط ويقلب الصفوف
 * (المبلغ يمينًا والاسم يسارًا). وحتى قراءة العلم هنا غير موثوقة: هذا الملف يُقيَّم
 * قبل تنفيذ جسم _layout.tsx، فيلتقط قيمة قديمة.
 *
 * لذا نرتّب الواجهة عربيًا صراحةً بهذه الثوابت (مع textAlign للنصوص)، فتكون النتيجة
 * واحدة على كل المنصات وقبل إعادة التشغيل وبعدها.
 *
 * استخدم ROW في أي صفّ يجب أن يبدأ من اليمين (أيقونة/اسم يمينًا ↔ مبلغ يسارًا).
 */

/** صفّ يبدأ من يمين الشاشة — أول عنصر في الـ JSX يظهر في أقصى اليمين. */
export const ROW = 'row-reverse' as const

/** محاذاة محتوى عمود إلى يمين الشاشة (alignItems). */
export const ALIGN_RIGHT = 'flex-end' as const

/** محاذاة محتوى عمود إلى يسار الشاشة (alignItems). */
export const ALIGN_LEFT = 'flex-start' as const
