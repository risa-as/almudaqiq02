# دليل النشر والاختبار قبل التأجير — SuperMarket Cloud

قائمة إلزامية قبل تأجير النظام لمؤسسة حقيقية. رتّبها بالأولوية.

---

## 1. قاعدة البيانات (إلزامي قبل أي عميل)
- [ ] طبّق الـ migrations على الإنتاج: `npx prisma migrate deploy`
      (يشمل `20260603_add_credit_limit_token_version_feature_overrides`).
- [ ] ⚠️ **خطتك مجانية = لا PITR.** لذا النسخ المنطقية إلزامية الآن:
  - [ ] ثبّت `pg_dump` وجدول `npm run backup:db` يومياً (Task Scheduler). انظر `BACKUP.md`.
  - [ ] فعّل **نسخة خارجية** (مجلد سحابي مُزامَن أو `BACKUP_UPLOAD_CMD`) — حتى لا يضيع كل شيء بتلف القرص.
- [ ] **اختبر استرجاعاً فعلياً** على فرع Neon تجريبي مرة واحدة على الأقل.
- [ ] عند ترقية الخطة لاحقاً: فعّل **PITR** من لوحة Neon (تكامل فوري بلا تغيير كود — انظر `BACKUP.md` §6).

## 2. الأسرار (إلزامي)
- [ ] دوّر كل الأسرار الخمسة في بيئة الإنتاج (ليست نفس قيم التطوير إن أمكن):
      `JWT_SECRET`, `REFRESH_TOKEN_SECRET`, `BRANCH_TOKEN_SECRET`, `LICENSE_SECRET_KEY`, `DEVELOPER_PASSWORD`.
- [ ] تأكد أن `DEVELOPER_PASSWORD` ≥ 16 حرفاً وقوي.
- [ ] لا يوجد `.env` أو `.env.bak` في git (مُتحقَّق — متجاهلان).

## 3. تشفير أجهزة الكاشير (إلزامي — بيانات SQLite غير مشفّرة)
قاعدة `dev.db` على جهاز الكاشير غير مشفّرة. فعّل **BitLocker**:
1. لوحة التحكم → BitLocker Drive Encryption → تشفير قرص النظام (C:).
2. احتفظ بمفتاح الاسترداد في مكان آمن.
> بديل متقدّم لاحقاً: SQLCipher (يتطلب تغييراً برمجياً في طبقة Prisma — ليس الآن).

## 4. اختبار حي إلزامي للمزامنة (الأخطر)
- [ ] `npm run verify:sync` → يجب أن يطبع **PASS** (يتحقق من خصم مخزون السحابة منطقياً).
- [ ] **دورة كاملة حية**:
  1. افصل الإنترنت عن جهاز الكاشير.
  2. نفّذ عدة مبيعات (نقد + دين + شبكة).
  3. أعد الإنترنت → انتظر المزامنة التلقائية.
  4. **قارن**: كمية كل صنف + رصيد كل عميل + إجمالي المبيعات → السحابة = المحلي.
  5. تأكد أن ترتيب الفواتير في السحابة صحيح وأن الأسعار وقت البيع محفوظة.

## 5. اختبار الأجهزة (لا يمكن فحصه برمجياً)
- [ ] طباعة إيصال على الطابعة الحرارية فعلياً.
- [ ] مسح باركود بسرعة كافية في نقطة البيع.
- [ ] شاشة فاحص الأسعار للعميل.
- [ ] التحديث التلقائي لـ Electron → تثبيت إصدار جديد **دون فقدان `dev.db`**.

## 6. الصلاحيات والخطط
- [ ] كاشير: لا يصل لتعديل المنتجات/الأسعار (تأكّد عبر محاولة فعلية).
- [ ] مدير فرع: لا يرى بيانات فرع آخر.
- [ ] مستأجر على "الأساسية": الصفحات المقفلة تُظهر شاشة الترقية.
- [ ] تجاوز حدّ الدين لعميل → يُمنع البيع الآجل برسالة واضحة.
- [ ] المخزون لا ينزل تحت الصفر (حاول بيع كمية أكبر من المتوفر → يُرفض).

## 7. الاستقرار (اختبار تحمّل)
- [ ] تشغيل Electron 8 ساعات متواصلة → لا بطء/تعليق.
- [ ] إدخال 200+ فاتورة في جلسة → الأداء مقبول.
- [ ] التطبيق يعمل على Windows 10 و 11.

## 8. التقارير
- [ ] أرقام تقرير المبيعات = مجموع الفواتير الفعلية.
- [ ] ذمم العملاء (30/60/90 يوم) صحيحة.
- [ ] تصدير PDF/Excel يعمل بلا أخطاء.

---

## 🚦 خطة الإطلاق الموصى بها
1. أنجز الأقسام 1–4 (إلزامية).
2. **عميل تجريبي واحد (Pilot)** أسبوعاً كاملاً — راقب المزامنة والتقارير يومياً.
3. أصلح ما يظهر، ثم وسّع تدريجياً (2–3 عملاء)، ثم إطلاق عام.

> القاعدة الذهبية: **لا تطلق على عدة مؤسسات دفعة واحدة قبل أسبوع pilot ناجح.**
