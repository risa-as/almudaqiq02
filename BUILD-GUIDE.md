# دليل التشغيل والبناء — نظام المدقق

يشرح هذا الدليل ثلاثة أشياء:

1. تشغيل **موقع الويب** (النسخة السحابية) محلياً وعلى الإنتاج
2. تشغيل **تطبيق سطح المكتب** محلياً للتطوير
3. **بناء نسخة توزيع** (مُثبِّت `.exe`) لتطبيق سطح المكتب

> راجع أيضاً قسم [قاعدة البيانات المشحونة](#قاعدة-البيانات-المشحونة) — يشرح ما
> الذي يُشحن داخل المُثبِّت وكيف تتأكد منه.

---

## 0. المتطلبات

| المتطلب | التفاصيل |
|---|---|
| Node.js | 18 أو أحدث (المثبَّت لديك: 22) |
| نظام التشغيل للبناء | **Windows** — هدف البناء هو `nsis` (مُثبِّت ويندوز) |
| أدوات C++ | **غير مطلوبة.** الوحدة الأصلية الوحيدة (`bcrypt`) تُشحن مبنيةً مسبقاً بـ N-API، و `npmRebuild: false` يمنع محاولة إعادة بنائها |
| ملف `.env` | موجود في جذر المشروع (غير مرفوع إلى git) |
| اتصال إنترنت | مطلوب في أول بناء فقط لتنزيل ثنائيات Electron |

---

## 1. فهم البنية أولاً (مهم قبل أي أمر)

المشروع الواحد يعمل بوضعين مختلفين تماماً، ويُفرَّق بينهما بمتغيّر البيئة `IS_ELECTRON`:

| | وضع السحابة (الويب) | وضع سطح المكتب (Electron) |
|---|---|---|
| `IS_ELECTRON` | غير مضبوط | `1` |
| قاعدة البيانات | PostgreSQL على Neon (فرانكفورت) | SQLite محلي (`dev.db`) |
| مخطط Prisma | `prisma/schema.prisma` | `prisma/schema.local.prisma` |
| عميل Prisma | `@prisma/client` | `@prisma/client-local` |
| المصادقة | كوكيز آمنة (HTTPS) | كوكيز عادية (`http://localhost`) |
| مجلد البناء | `.next` / `.next-cloud` | `.next-build` |

`lib/prisma.ts` هو من يختار العميل الصحيح وقت التشغيل، لذلك **نفس الكود** يخدم الوضعين.

تطبيق سطح المكتب ليس تطبيقاً منفصلاً: هو Electron يشغّل خادم Next.js مستقلاً
(standalone) داخل عملية فرعية على `localhost:3000`، ويفتح نافذة متصفح عليه.

---

## 2. تشغيل موقع الويب

### 2.1 محلياً (تطوير)

```bash
npm install          # أول مرة فقط
npm run dev          # http://localhost:3000
```

هذا يستخدم `DATABASE_URL` من `.env` — أي **قاعدة بيانات السحابة مباشرة**. انتبه:
أي تعديل هنا يمسّ بيانات الإنتاج إن كان `DATABASE_URL` يشير إلى Neon الإنتاجي.

### 2.2 محلياً بالتوازي مع تطبيق سطح المكتب

إن أردت تشغيل الويب وسطح المكتب معاً على نفس الجهاز، شغّل الويب على منفذ آخر
وبمجلد بناء منفصل حتى لا يتصادما:

```bash
npm run dev:cloud    # http://localhost:3001  (مجلد البناء .next-cloud)
```

هذا هو السبب الوحيد لوجود هذا الأمر: `npm run dev` و Electron كلاهما يريد
المنفذ 3000 ومجلد `.next`.

### 2.3 على الإنتاج (Vercel)

النشر يتم تلقائياً عند الدفع إلى الفرع المرتبط بـ Vercel. الإعدادات الحرجة:

- **المنطقة**: `fra1` (فرانكفورت) — مضبوطة في `vercel.json`، ويجب أن تبقى
  مطابقة لمنطقة قاعدة بيانات Neon، وإلا صار كل استعلام يعبر المحيط.
  تحقّق منها بـ:
  ```bash
  curl -sI https://almudaqiq02.com/api/cron/daily-digest | grep -i x-vercel-id
  # الرمز الثاني يجب أن يكون fra1
  ```
- **المهام المجدولة** (`crons` في `vercel.json`):
  | المسار | التوقيت | الوظيفة |
  |---|---|---|
  | `/api/cron/check-subscriptions` | `0 5 * * *` | نقل الاشتراكات ACTIVE → GRACE → SUSPENDED وإرسال تنبيهات 7 أيام / يوم واحد |
  | `/api/cron/daily-digest` | `0 6 * * *` | الملخص اليومي الذكي |

  الترتيب مقصود: فحص الاشتراكات يسبق الملخص بساعة حتى يقرأ الملخص حالات محدَّثة.
- **`CRON_SECRET` إلزامي** في متغيّرات بيئة Vercel، وإلا رجّعت المهمتان 401
  ولم يحدث شيء إطلاقاً (بصمت).

---

## 3. تشغيل تطبيق سطح المكتب (تطوير)

### 3.1 التحضير — مرة واحدة، أو بعد أي تعديل على `schema.local.prisma`

```bash
npm run prisma:local   # يولّد @prisma/client-local من المخطط المحلي
npm run db:push        # ينشئ/يحدّث prisma/dev.db حسب المخطط
```

> **مصيدة مسارات — انتبه لها:** واجهة Prisma تحسب مسار `file:` **نسبةً إلى مجلد
> ملف المخطط** (`prisma/`)، لا إلى جذر المشروع. لذلك القيمة الصحيحة هي
> `file:./dev.db` (تُحلّ إلى `prisma/dev.db`).
>
> كان السكربت يمرّر `file:./prisma/dev.db`، فكان يكتب فعلياً إلى
> `prisma/prisma/dev.db` — ملف تائه لا يقرأه التطبيق ولا يُشحن. النتيجة: أي
> تعديل على `schema.local.prisma` لا يصل إلى قاعدة البيانات المشحونة، ويظهر
> الخلل عند العميل فقط بخطأ `P2022 column ... does not exist`.
> **أُصلح في `package.json` (2026-07-28).** إن وجدت مجلد `prisma/prisma/`
> فاحذفه، فهو من قبل الإصلاح.

### 3.2 التشغيل

```bash
npm run dev:electron
```

يقوم هذا الأمر بثلاثة أشياء بالترتيب:

1. يقتل أي عملية على المنفذ 3000
2. يشغّل `next dev` بوضع `IS_ELECTRON=1`
3. ينتظر جاهزية `localhost:3000` ثم يفتح نافذة Electron

بدلاً منه يمكن تشغيل الاثنين يدوياً في طرفيتين:

```bash
# طرفية 1
cross-env IS_ELECTRON=1 LOCAL_DATABASE_URL=file:./prisma/dev.db next dev
# طرفية 2
npm run electron
```

### 3.3 تسجيل الدخول أول مرة

تطبيق سطح المكتب **يحتاج إنترنت في أول تسجيل دخول فقط**: يتحقق من الحساب
والاشتراك عبر `CLOUD_URL/api/auth/desktop-verify`، ثم يخزّن النتيجة محلياً
ويعمل بعدها دون إنترنت لمدة تصل إلى 30 يوماً قبل أن يطلب تحققاً جديداً.

---

## 4. بناء نسخة التوزيع (المُثبِّت)

### 4.1 قبل البناء — تحقّق من هذه النقاط

1. **`CLOUD_URL` في `.env`** يجب أن يكون عنوان الإنتاج، لا `localhost`:
   ```bash
   grep "^CLOUD_URL" .env
   # المتوقع: CLOUD_URL="https://almudaqiq02.com"
   ```
   هذه القيمة تُكتب داخل المُثبِّت وهي الوحيدة التي تُشحن من `.env`. إن كانت
   خاطئة **لن يستطيع أي عميل تسجيل الدخول** لأن التحقق الأول لن يجد الخادم.

2. **قاعدة بيانات الشحن** — راجع [التحذير في الأسفل](#تحذير-قاعدة-البيانات-المشحونة).

3. أغلق أي نسخة تعمل من التطبيق (المُثبِّت لا يستطيع الكتابة فوق ملفات مقفلة).

### 4.2 أمر البناء

```bash
npm run dist
```

**هذا كل شيء.** المُخرَج النهائي:

```
dist/المدقق Setup <version>.exe     ← المُثبِّت الذي تعطيه للعميل
dist/win-unpacked/                   ← نسخة غير مضغوطة للفحص والتجريب
```

### 4.3 شرح ما يحدث داخلياً (ست مراحل)

`npm run dist` ليس أمراً واحداً بل سلسلة:

```
prisma:local → db:push → clean:dist → build:electron → postbuild → electron-builder
```

| # | المرحلة | ماذا تفعل | لماذا |
|---|---|---|---|
| 1 | `prisma:local` | يولّد `@prisma/client-local` | العميل الذي يتحدث مع SQLite — بدونه لا يعمل التطبيق ولا المزامنة |
| 2 | `db:push` | يطبّق `schema.local.prisma` على `prisma/dev.db` | القاعدة التي ستُشحن يجب أن تطابق المخطط الحالي |
| 3 | `clean:dist` | يحذف `dist/` و `.next-build/` | يمنع خلط ملفات بناء قديمة بجديدة |
| 4 | `build:electron` | `next build` بوضع `IS_ELECTRON=1` ومجلد `.next-build` | ينتج خادم Next.js standalone |
| 5 | `postbuild` | `scripts/prepare-standalone.js` | يجمع القطع التي لا يجمعها Next تلقائياً |
| 6 | `electron-builder` | يحزم كل شيء في مُثبِّت nsis | المنتج النهائي |

**تفصيل المرحلة 5** (`prepare-standalone.js`) — أكثر مرحلة يقع فيها الخطأ:

- ينسخ `public/` و `.next-build/static/` إلى مجلد standalone
- ينسخ `electron/` (عامل المزامنة وطابور العمل دون اتصال)
- ينسخ `@prisma/client-local` إلى `node_modules` داخل standalone
- يبني **قاعدة بيانات شحن نظيفة** عبر `scripts/make-clean-db.js` (بنية الجداول
  فقط، صفر صفوف) وينسخها، ثم **يتحقق منها ويوقف البناء** إن وجد أي صف في
  `User` أو `SuperAdmin` أو `RefreshToken` أو `SyncQueue`
- يكتب ملف `.env` مصغّر يحوي `CLOUD_URL` فقط — **بلا أي أسرار سحابية**
  (أسرار JWT تُولَّد عشوائياً لكل تثبيت من `electron/main.js` عند أول تشغيل،
  ولا تُشحن أبداً)

وفي آخر المرحلة 5 يعمل **حارس التحقق** (`verifyNothingSecretIsShipped`) الذي
يفحص الحزمة قبل تسليمها إلى electron-builder، ويُفشل البناء إن وجد أي سرّ في
`.env` المشحون أو أي صف في أي قاعدة بيانات داخل الحزمة. المخرَج المتوقع:

```
Shipping bundle verified — no secrets, no populated database.
```

> **مصيدة `NEXT_DIST_DIR` — سبب وجود الحارس أعلاه.**
> `build:electron` يبني إلى `.next-build`، و electron-builder يشحن
> `.next-build/standalone` (انظر `extraResources`). لكن `prepare-standalone.js`
> يقرأ `process.env.NEXT_DIST_DIR` ويرجع إلى `.next` عند غيابه — وكان `dist`
> يستدعيه عبر `npm run postbuild` **بلا** تمرير المتغيّر.
>
> النتيجة: كل عمل المرحلة 5 كان يُكتب في `.next/standalone` (مجلد غير مشحون
> أصلاً)، بينما المُثبِّت يأخذ ما وضعه Next وحده في `.next-build/standalone`،
> أي **ملف `.env` الكامل للمشروع** و**قاعدة بيانات المطوّر** — ولا يظهر في
> سجل البناء أي خطأ. أُصلح بتمرير `NEXT_DIST_DIR=.next-build` صراحةً في `dist`.

**تفصيل المرحلة 6**: `scripts/after-pack.cjs` ينسخ `node_modules` الخاصة بخادم
standalone عبر `robocopy` بدل النسخ العادي، لأن Windows Defender كثيراً ما يقفل
ثنائيات Prisma الحديثة مؤقتاً — و `robocopy` يعيد المحاولة تلقائياً.

### 4.4 التحقق بعد البناء

```bash
ls dist/*.exe                                          # المُثبِّت موجود
ls dist/win-unpacked/resources/server/dev.db           # قاعدة البيانات مشحونة

# ▼ الفحص الأمني — يجب أن تكون النتيجة صفراً في الحالتين
grep -cE "^(DATABASE_URL|JWT_SECRET|LICENSE_PRIVATE_KEY|CRON_SECRET)=" \
  dist/win-unpacked/resources/server/.env
ls dist/win-unpacked/resources/server/prisma/dev.db 2>/dev/null | wc -l
```

الملف `.env` المشحون يجب أن يكون بأربعة أسطر فقط:
`NODE_ENV` و `IS_ELECTRON` و `LOCAL_DATABASE_URL` و `CLOUD_URL`. أي سطر آخر =
تسريب. (الحارس في المرحلة 5 يمنع ذلك تلقائياً، وهذا فحص يدوي مؤكِّد.)

ثم ثبّت النسخة على جهاز اختبار وجرّب:

1. تسجيل دخول أول مرة (يتطلب إنترنت)
2. قطع الإنترنت والتأكد من استمرار العمل
3. الإعدادات ← الاشتراك ← زر **«تحديث الحالة»**

---

## 5. مشاكل شائعة

| الرسالة / العرَض | السبب | الحل |
|---|---|---|
| `WARNING: @prisma/client-local not generated!` | شغّلت `npm run build` بدل `npm run dist` | استخدم `npm run dist`، أو شغّل `npm run prisma:local` أولاً |
| `WARNING: CLOUD_URL is not set or points to localhost` | `.env` فيه عنوان تطوير | صحّح `CLOUD_URL` وأعد البناء — النسخة المبنية غير صالحة للعميل |
| `robocopy failed` | Defender يقفل ملفات Prisma | أعد الأمر، أو استثنِ مجلد المشروع من الفحص اللحظي |
| `node-gyp failed to rebuild 'bcrypt'` + `Could not find any Visual Studio installation` | electron-builder كان يحاول إعادة بناء `bcrypt` من المصدر، وهو يتطلب أدوات C++ من Visual Studio | **أُصلح**: `npmRebuild: false` في `package.json`. `bcrypt` v6 يستخدم N-API وملفاته المبنية مسبقاً (`prebuilds/win32-x64`) تعمل تحت Electron كما تعمل تحت Node — إعادة البناء كانت بلا فائدة أصلاً. لا حاجة لتثبيت Visual Studio |
| المُثبِّت لا يُكتب فوقه | نسخة من التطبيق تعمل | أغلق التطبيق تماماً (تحقق من Task Manager) |
| المنفذ 3000 مشغول | خادم تطوير قديم | `npx kill-port 3000` |
| التطبيق يفتح على شاشة بيضاء | خادم standalone لم يبدأ | افحص سجلات Electron — غالباً `dev.db` مفقودة أو تالفة |
| `P2022 column ... does not exist` عند العميل | تعديل على `schema.local.prisma` لم يصل إلى `prisma/dev.db` | تأكد أن `db:push` يستخدم `file:./dev.db` واحذف `prisma/prisma/` إن وُجد، ثم أعد البناء |
| البناء يتوقف عند `قاعدة البيانات المُراد شحنها ليست نظيفة` | فحص الأمان اكتشف صفوفاً في نسخة الشحن | هذا هو السلوك المطلوب — لا تتجاوزه؛ افحص `scripts/make-clean-db.js` |

---

## 6. المشروع الثاني — نسخة الأوف لاين

`D:\Programming\SuperMarket - only desktop` مشروع منفصل تماماً (يعمل بلا إنترنت
إطلاقاً، ويُفعَّل بمفتاح Ed25519 من `/super-admin/offline-licenses`). بناؤه:

```bash
npm run dist
```

الفروقات في البناء:

- لا يحتاج `CLOUD_URL` إطلاقاً — لا يتصل بأي خادم
- يحتاج مفتاح ترخيص Ed25519 عند أول تشغيل عند العميل، يُولَّد من
  `https://almudaqiq02.com/super-admin/offline-licenses` ويُربط ببصمة جهاز
  العميل (عنوان MAC) — اطلبها منه من شاشة التفعيل
- يستخدم نفس آلية قاعدة بيانات الشحن النظيفة (`make-clean-db.js`)، وهي أصل
  النسخة الموجودة الآن في المشروع السحابي

---

## قاعدة البيانات المشحونة

**كان** `prepare-standalone.js` ينسخ `prisma/dev.db` كما هي إلى المُثبِّت. وفحص
ذلك الملف أظهر أنه ليس فارغاً إطلاقاً — أي أن كل عميل كان سيستلم:

| الجدول | الصفوف | الخطورة |
|---|---|---|
| `User` | 4 | حسابات بكلمات مرور مُجزّأة — قابلة لتسجيل الدخول فعلياً |
| `RefreshToken` | 5 | رموز تحديث صالحة حتى 30 يوماً |
| `SyncQueue` | 58 | **عمليات معلّقة تُدفع إلى السحابة عند أول مزامنة** |
| `Transaction` / `TransactionItem` | 32 / 72 | فواتير حقيقية |
| `AuditLog` | 51 | سجل تدقيق |
| `Tenant` / `Branch` | 1 / 2 | بيانات مستأجر آخر |
| `Customer` / `Supplier` / `Product` | 5 / 3 / 8 | بيانات تشغيلية |

**تم إصلاح هذا (2026-07-28).** صار البناء يمرّ عبر `scripts/make-clean-db.js`:

1. ينسخ `prisma/dev.db` إلى ملف عمل منفصل — **قاعدة بيانات المطوّر تُقرأ فقط
   ولا تُعدَّل أبداً**
2. يحذف كل صف من كل جدول في النسخة، ثم `VACUUM`
3. يعيد وضع المجلة إلى `DELETE` فيصبح الناتج ملفاً واحداً بلا `-wal`/`-shm`
4. يتحقق من الملف الذي سيُشحن فعلاً، و**يُفشل البناء** إن وجد أي صف في
   `User` أو `SuperAdmin` أو `RefreshToken` أو `SyncQueue`

المخرَج المتوقع أثناء البناء:

```
Building clean shipping database (no accounts, no business data)...
[make-clean-db] قاعدة بيانات شحن نظيفة جاهزة (33 جدول، صفر صفوف)
Verified clean database — User=0 SuperAdmin=0 RefreshToken=0 SyncQueue=0
```

لا حاجة لأي خطوة يدوية. ولإنشاء قاعدة نظيفة يدوياً للفحص:

```bash
node scripts/make-clean-db.js
```

> **لا تستخدم `scripts/reset-operational-data.js` لهذا الغرض.** ذلك السكربت
> يعمل على `@prisma/client` أي **قاعدة بيانات السحابة (PostgreSQL)** لا على
> `dev.db` المحلية، وهو يُبقي `User` و `RefreshToken` و `Tenant` عمداً. تشغيله
> ظنّاً أنه ينظّف نسخة الشحن يحذف بيانات الإنتاج التشغيلية ولا ينظّف شيئاً.

### لماذا صفر صفوف وليس بذوراً افتراضية

تطبيق سطح المكتب لا يحتاج أي بذرة محلية: أول تسجيل دخول يتحقق من السحابة ويكتب
`Tenant` و `Branch` و `User` محلياً، وعامل المزامنة يسحب الباقي. وكل جدول يبدو
"مرجعياً" (مثل `Category`) مرتبط بـ `tenantId`، فلا معنى لشحنه بلا مستأجر.
